# --- PERSONAL DATA - never commit ---
data/profile.json
data/removal_status.json
docs/REMOVAL_REPORT.md
data/optery_sitemap.xml
*.local.json
.claude/settings.local.json
screenshots/
__pycache__/
*.pyc
outbox/
data/url_health.json
docs/MANUAL_CHECKLIST.md
.venv/
uv.lock
data/handoff_queue.json

# Raw state-registry CSVs: public government records, but bulk files of named
# individuals' work addresses. The importer refetches them with --fetch, so the
# derived rows and the script are enough for reproducibility without this repo
# republishing the bulk file.
data/registries/*.csv
