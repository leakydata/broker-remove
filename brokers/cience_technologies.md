# Cience Technologies Inc

- **Opt-out:** none published
- **Email:** ~~privacy@cience.com~~ — **hard-bounces 550 5.1.1, do not use**
- **Method:** email — but see below; hold out of the send queue
- **Domain:** cience.com
- **Priority: 1.** Duplicate of `graph8` (id `graph8`), already handled.

## Status

- Current: `unreachable` under this name — **duplicate of graph8, do not resend**
- Note (2026-09-12): A letter sent to `privacy@cience.com` hard-bounced (550
  5.1.1). Checking cience.com directly found no working contact — its
  `/privacy-policy` path now just redirects to `/terms` with no privacy
  contact in the page at all. graph8.com's own footer reads *"Tenbound
  defines the model. graph8 runs the system. CIENCE delivers the outcome,"*
  and describes itself as built on "a decade of CIENCE campaign performance"
  — CIENCE is now the legacy brand of the same operator as `graph8`.
  `graph8` already has a verified, working address (`support@graph8.com`)
  and an existing submitted request that was answered within a day (see
  `data/dead_addresses.json` → `privacy@graph8.com`).

## Steps

**Do not open a new request here.** This is the same pattern as
`terminus` → `demandscience`: one operator, registered twice under
different brand names. Raise anything CIENCE-specific inside the existing
graph8 thread rather than starting a second one — a second thread just
gives the company two tickets to reconcile instead of one to answer.

## Gotchas

- The Optery-sourced listing (`cience_technologies`) predates the rebrand
  and still points at the dead `cience.com` address. If this keeps
  resurfacing in future batches under the CIENCE name, that's this
  duplicate-detection note not being read — check `duplicate_of` in the
  registry before queuing it.

## Verification

Verify against the graph8 thread, not this one. Nothing new was submitted
under the CIENCE name.
