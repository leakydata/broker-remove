# Media.net

- **Email:** privacy@media.net (verified)
- **Method:** email — Statutory request by email. No web form needed.
- **Domain:** media.net
- **Priority: 2.**

## Status

- Current: `submitted` (updated 2026-08-29)
- Note: Emailed privacy@media.net 2026-08-28 (CA registry 2024-2026). CONTEXTUAL AD NETWORK -- conceded the obvious first, because the concession is what makes the rest answerable: contextual advertising works from the page, not the person, so a name search returns nothing because the name was never a key, and if that is the whole answer I will record it as a real result. Four asks: (1) is there an identifier-keyed store at all (cookie IDs, IDFA/AAID, hashed email, IP-derived household geo, segments), and is any of it linkable by them or a partner to a name; 'keyed only to identifiers the consumer cannot produce' accepted as an answer. (2) THE HASHED-EMAIL WEDGE, same move as the MaxMind letter: I cannot read a cookie ID and would not send one if I could, but the ad-tech identity layer is keyed to hashes of email addresses and I have twelve of those -- so hash them YOURSELF, lowercased and trimmed, SHA-256 plus MD5/SHA-1 if held, and search. Derives nothing they could not already derive and hands over nothing new. (3) WHERE THE OPT-OUT LIVES -- the sharpest question: if the mechanism is a browser cookie then it is scoped to one browser on one device, absent from the phone, and destroyed by a cache clear, which is to say it is stored inside the thing it protects against; asked whether a server-side opt-out persists against an identifier, and said plainly that 'we honour opt-outs' and 'we set a cookie' are different claims. (4) GPC -- honoured, and recorded persistently or only in-session? No IP address sent, with the reason given.

## Steps

<!-- Replace once the route is confirmed. What actually worked, in order. -->

## Gotchas

<!-- Fill in from their reply. Recurring things worth capturing:
     - Do they refuse email and point at a form? Which form?
     - Is a CAPTCHA on page load (blocks automation) or at submit (can hand off)?
     - Does the form silently drop values not committed with an Add/+ button?
     - Do they gate on state of residence? Does their own form contradict that?
     - What does the removal NOT cover — name search only? FCRA-exempt products?
     - Any upsell to a paid removal service? -->

## Verification

<!-- How to check it worked: the search URL to re-run, and their stated timeframe. -->
