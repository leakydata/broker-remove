# Persistent Id

- **Email:** privacy@persistent.id (verified)
- **Method:** email — Statutory request by email. No web form needed.
- **Domain:** persistent.id
- **Priority: 2.**

## Status

- Current: `submitted` (updated 2026-08-19)
- Note: Contact corrected before sending: their published address is privacy@persistent.id, not the info@ the registry held. Identity-resolution letter built around the company's own premise - a persistent identifier is valuable precisely because it outlives the identifiers a person can change, so a deletion scoped to the name row leaves the part that matters. Asked for the minted ID itself and every mapping to it, hashed emails, cookie/MAID/CTV IDs, IP-household links, derived segments, and THE EDGES. Four questions: which identifier types they matched on; whether an ID already distributed to customers is REVOKED downstream or merely not reissued (only the first is a removal); processor vs controller; and whether reappearing under the same email would mint a fresh ID and rebuild the profile - if so, a permanent do-not-resolve entry is what is actually needed.

## Steps

<!-- Replace once the route is confirmed. What actually worked, in order. -->

## Gotchas

<!-- Fill in from their reply. Recurring things worth capturing:
     - Do they refuse email and point at a form? Which form?
     - Is a CAPTCHA on page load (blocks automation) or at submit (can hand off)?
     - Does the form silently drop values not committed with an Add/+ button?
     - Do they gate on state of residence? Does their own form contradict that?
     - What does the removal NOT cover — name search only? FCRA-exempt products?
     - Any upsell to a paid removal service? -->

## Verification

<!-- How to check it worked: the search URL to re-run, and their stated timeframe. -->
