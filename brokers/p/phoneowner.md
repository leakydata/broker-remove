# Phoneowner Inc

- **Email:** contact@phoneowner.com (verified)
- **Method:** email — Statutory request by email. No web form needed.
- **Domain:** phoneowner.com
- **Priority: 1.**

## Status

- Current: `unreachable` (updated 2026-09-01)
- Note: 2026-09-01 CORRECTION — this was previously recorded as `submitted`, written right after the send and before the bounce arrived. contact@phoneowner.com hard-bounced (550 5.1.1, address not found) the same day. No alternate address published anywhere; the domain has live MX (proves the domain accepts mail for *some* mailbox, not this one — see `_SILENT_FAILURES.md` §236/237). Re-verify the company is actually a data broker before spending another send here: this row came from a third-party removal-service directory, not a state registration, so nobody has attested to what it is.
- **Re-confirmed (2026-09-03):** checked whether the site itself offers any other route. It's live, but the only contact channel (phoneowner.com/contact/) is a form whose mailto action is literally `contact@phoneowner.com` — the same dead address, just wrapped in a form. The privacy-policy page has no email at all ("if you write to us...", no address given). Confirmed via raw HTML rather than a rendered summary. No alternate address exists anywhere on the site.
- Note (original, kept for the reasoning): Directory-sourced, and the highest-value row in the residue cohort: PhoneOwner is a reverse PHONE lookup, i.e. a genuine people-search keyed on telephone number rather than name. Full CCPA request -- delete (1798.105), opt-out of sale/share (1798.120) incl. affiliates, syndication partners, API customers and bulk licensees, and categories disclosure (1798.110/.115). PA resident framing: asked them to apply it as ordinary practice and to say plainly if they will not, rather than let it lapse. LISTING-URL ASK PUT FIRST (SF 185): they publish reverse-lookup results as pages, so I asked for the URL of every page returning info about me, keyed by phone, name OR address. Reasoning given in the letter: I cannot verify a removal I cannot see -- without the URL I cannot re-check after the next refresh. Explicitly said 'no such pages' is also a useful answer and I would record it as theirs rather than my inference. SUPPRESS-NOT-DELETE argument carried in full: a reverse-lookup index rebuilds from upstream feeds, so a bare deletion is undone by the next ingest BECAUSE NOTHING REMAINS THAT RECOGNISES the person as having asked. Asked for a minimum suppression key used for nothing else, cited 1798.105(d)(1), and asked for confirmation it survives refresh + migration + acquisition (SF 224). Phone number led the identifier block since that is their index key. SF 193 limit stated (suppress the ASSOCIATION, other people live at those addresses now) and the common-name discriminator with DOB offered only to RULE OUT. SF 235 entity-name question included. No SSN, no government ID -- and said so, with the argument that a reverse phone lookup needs neither.

## Steps

<!-- Replace once the route is confirmed. What actually worked, in order. -->

## Gotchas

<!-- Fill in from their reply. Recurring things worth capturing:
     - Do they refuse email and point at a form? Which form?
     - Is a CAPTCHA on page load (blocks automation) or at submit (can hand off)?
     - Does the form silently drop values not committed with an Add/+ button?
     - Do they gate on state of residence? Does their own form contradict that?
     - What does the removal NOT cover — name search only? FCRA-exempt products?
     - Any upsell to a paid removal service? -->

## Verification

<!-- How to check it worked: the search URL to re-run, and their stated timeframe. -->
